import urllib.request
import json
import base64

# Try to find the image in the user's uploaded files... wait, I can't read the user's uploaded files directly.
# BUT I can just make a test HTML page and serve it, or I can just print out the math!
